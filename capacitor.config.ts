import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ziainfo.nfcwithionic',
  appName: 'IonNfc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
